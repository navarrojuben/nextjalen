/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://nextjalen.vercel.app',
  generateRobotsTxt: true, // (optional) generate robots.txt
  sitemapSize: 7000,
  changefreq: 'weekly',
  priority: 0.7,
  // You can also add exclude, additionalPaths, etc.
};
